/* export const calculateEstimation = async (
    origin: string,
    destination: string,
  ): Promise<number> => {
    try {
      // Simulação do cálculo (substituir pela API do Google Maps futuramente)
      const basePrice = 10.0; // Valor base
      const distanceFactor = 2.5; // Fator de multiplicação por distância
  
      const distance = Math.random() * 10 + 1; // Simula uma distância aleatória entre 1km e 10km
      const price = basePrice + distance * distanceFactor;
  
      return parseFloat(price.toFixed(2));
    } catch (error) {
      console.error("Erro no cálculo de estimativa:", error);
      throw new Error("Erro no cálculo de estimativa");
    }
  };
   */